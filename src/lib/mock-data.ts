import type { Campaign, SessionPrep } from './types';

// This file is now mostly deprecated in favor of local storage,
// but can be useful for seeding initial data or for testing.

export const mockCampaigns: Campaign[] = [];
